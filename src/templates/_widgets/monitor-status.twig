<div
    class="upsnap-monitor-widget"
    data-endpoint="{{ endpoint }}"
    data-dashboard-url="{{ dashboardUrl }}"
    data-monitors-url="{{ monitorsUrl }}"
    data-settings-url="{{ settingsUrl }}"
    data-upgrade-url="{{ upgradeUrl }}"
>
    <div class="upsnap-monitor-widget__state" data-upsnap-widget-state>
        {{ 'Loading monitors...'|t('upsnap') }}
    </div>
</div>
